<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PipelineDate__c</fullName>
    <description>A trick only for Pipeline records to only display pipeline information in either today&#39;s or this months report, hiding it from reports if you try and pull old data as the pipeline is not being snap shot.</description>
    <externalId>false</externalId>
    <formula>TODAY()</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Pipeline Date</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
