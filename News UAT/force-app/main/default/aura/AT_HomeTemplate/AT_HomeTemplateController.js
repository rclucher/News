({
	doInit: function(component, event, helper) {
		/*component.find('messageOverlay').showCustomPopover({
            title: "Importand Message",
            body: "Test Message",
            referenceSelector: ".messageOverlay",
            cssClass: "slds-popover"
        }).then(function (overlay) {
            setTimeout(function(){ 
                //close the popover after 3 seconds
                overlay.close(); 
            }, 3000);
        });
        
        component.find("popuplib").notifyClose();*/
	}
})